name: HR Attrition Preprocessing Pipeline

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  preprocessing:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run preprocessing script
        run: |
          python preprosessing/automate_Nolan_Mahotama_Abipantara_Urya_Gotama.py

      - name: Upload processed data
        uses: actions/upload-artifact@v4
        with:
          name: processed-hr-attrition
          path: HR-Employee-Attrition_preprosessing/*.csv
